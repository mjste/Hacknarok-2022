import { SafeAreaView, Text, View } from 'react-native'
import { DaysGrid } from './DaysGrid'

const Home = () => {
	return (
		<SafeAreaView>
			<DaysGrid />
		</SafeAreaView>
	)
}

export default Home
